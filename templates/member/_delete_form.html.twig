<form method="post" action="{{ path('member_delete', {'id': cp_member.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cp_member.id) }}">
    <button class="btn">Delete</button>
</form>
